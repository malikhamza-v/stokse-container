server {
    listen 80;
    server_name app.stokse.com;

    location / {
        root /var/www/react;
        try_files $uri /index.html;
    }

    gzip on;
    gzip_types text/plain application/xml text/css application/javascript;
    gzip_vary on;
    gzip_min_length 256;
    gzip_proxied any;
    gzip_disable "msie6";

    keepalive_timeout 75;

    location ~ /\. {
        deny all;
    }

    client_max_body_size 50M;
}
